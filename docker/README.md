# ChestDiseaseAI Docker Deployment Configuration

This directory contains containerization files for building and running the ChestDiseaseAI stack:
- FastAPI Backend container (`Dockerfile.backend`)
- React/Next.js Frontend container (`Dockerfile.frontend`)
- Multi-container orchestration (`docker-compose.yml` in root)
